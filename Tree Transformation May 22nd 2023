//C++

class Solution {
    public:
    int solve(int N, vector<int> p){
        // code here
         unordered_set<int> s;
        for(int i = 0; i < N; i++){
            if(p[i] > 0)
                s.insert(p[i]);
        }   
        return s.size();
        
    }
};
